package com.payroc.transaction

class Transaction(val amount: Double, val orderId: String) {
    fun provideCard() {
        TODO("Read a random card from card_data.xml and update state")
        // Update State
        transactionRequest()
    }

    fun transactionRequest() {

    }
}